"use client";
import {useMemo,useState} from "react";
import {Check,ChevronDown,Search} from "lucide-react";
export type CatalogOption={value:string;label:string};
export function SearchableCatalogSelect({value,options,onChange,placeholder,searchable=true}:{value:string;options:CatalogOption[];onChange:(value:string)=>void;placeholder:string;searchable?:boolean}){const[open,setOpen]=useState(false),[query,setQuery]=useState("");const selected=options.find(o=>o.value===value);const filtered=useMemo(()=>options.filter(o=>o.label.toLowerCase().includes(query.toLowerCase())),[options,query]);return <div className="relative"><button type="button" onClick={()=>setOpen(v=>!v)} className="flex h-10 w-full items-center justify-between rounded-xl border border-slate-200 bg-white px-3 text-left text-sm font-semibold text-slate-700 outline-none focus:border-[#C5A059] focus:ring-2 focus:ring-[#C5A059]/10">{selected?.label??placeholder}<ChevronDown size={16} className="text-slate-400"/></button>{open&&<div className="absolute z-[100] mt-1 w-full rounded-xl border border-slate-100 bg-white p-2 shadow-xl">{searchable&&<div className="relative mb-1"><Search size={14} className="absolute left-3 top-2.5 text-slate-400"/><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} className="h-9 w-full rounded-lg border border-slate-200 bg-slate-50 pl-8 text-sm outline-none focus:border-[#C5A059]" placeholder="Buscar..."/></div>}<div className="max-h-52 overflow-y-auto">{filtered.map(o=><button type="button" key={o.value} onClick={()=>{onChange(o.value);setOpen(false);setQuery("")}} className="flex w-full items-center justify-between rounded-lg px-3 py-2 text-left text-sm hover:bg-[#C5A059]/10">{o.label}{o.value===value&&<Check size={14} className="text-[#C5A059]"/>}</button>)}</div></div>}</div>}
