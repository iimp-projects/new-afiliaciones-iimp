import { AlertTriangle, CheckCircle2, Clock3 } from "lucide-react";
import { getOperationalAlertSeverityLabel, getOperationalAlertStatusLabel } from "../Config/OperationalAlertCatalog";

export function OperationalAlertSeverityBadge({ severity }: { severity: string }) { const critical = severity === "CRITICAL"; return <span className={`inline-flex items-center gap-1 rounded-md border px-2 py-1 text-[10px] font-black ${critical ? "border-rose-100 bg-rose-50 text-rose-700" : "border-amber-100 bg-amber-50 text-amber-800"}`}><AlertTriangle size={12} />{getOperationalAlertSeverityLabel(severity)}</span>; }
export function OperationalAlertStatusBadge({ status }: { status: string }) { const config = status === "RESOLVED" ? { Icon: CheckCircle2, className: "border-emerald-100 bg-emerald-50 text-emerald-700" } : status === "IN_PROGRESS" ? { Icon: Clock3, className: "border-blue-100 bg-blue-50 text-blue-700" } : { Icon: AlertTriangle, className: "border-slate-200 bg-slate-100 text-slate-700" }; return <span className={`inline-flex items-center gap-1 rounded-md border px-2 py-1 text-[10px] font-black ${config.className}`}><config.Icon size={12} />{getOperationalAlertStatusLabel(status)}</span>; }
