import { NextRequest, NextResponse } from "next/server";
import { contextService } from "@/modules/auth/context/service";
import { MasterDataMergeService } from "@/modules/master-data/Services/MasterDataMergeService";
import type { MasterDataEntity } from "@/modules/master-data/Repositories/MasterDataRepository";
function parse(raw:string,rawId:string){const entity=(raw==="universities"?"UNIVERSITY":raw==="specialties"?"SPECIALTY":raw==="degrees"?"DEGREE":null) as MasterDataEntity|null,id=Number(rawId);if(!entity||!Number.isInteger(id)||id<=0)throw new Error("Identificador inválido.");return{entity,id}}
export async function GET(request:NextRequest,{params}:{params:Promise<{entity:string;id:string}>}){try{await contextService.requirePermission("read","catalogs");const p=await params,x=parse(p.entity,p.id),q=request.nextUrl.searchParams;return NextResponse.json({success:true,...(await new MasterDataMergeService().references(x.entity,x.id,Math.max(1,Number(q.get("page")||1)),[20,50,100].includes(Number(q.get("pageSize")))?Number(q.get("pageSize")):20,q.get("search")||""))})}catch(error){return NextResponse.json({success:false,message:error instanceof Error?error.message:"No autorizado."},{status:400})}}
export async function PATCH(request:NextRequest,{params}:{params:Promise<{entity:string;id:string}>}){try{const user=await contextService.getCurrentUser();await contextService.requirePermission("update","catalogs");if(!user)return NextResponse.json({success:false,message:"No autorizado."},{status:403});const p=await params,x=parse(p.entity,p.id),result=await new MasterDataMergeService().update(x.entity,x.id,{...(await request.json()),userId:user.id});return NextResponse.json({success:true,data:result})}catch(error){const status=error instanceof Error&&"status" in error?Number((error as {status:number}).status):400;return NextResponse.json({success:false,message:error instanceof Error?error.message:"No se pudo actualizar."},{status})}}
